{
  "name": "load-once",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/achingbrain/load-once.git"
  },
  "scripts": {
    "test": "NODE_ENV=test mocha",
    "test:coverage": "istanbul --include-all-sources cover _mocha",
    "test:coverage:check": "istanbul --include-all-sources cover _mocha && istanbul check-coverage --statement 100 --branch 100 --function 100",
    "lint": "standard",
    "pre-commit": "npm run lint && npm run test:coverage:check"
  },
  "author": "Alex Potsides",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.0.0",
    "istanbul": "^0.4.0",
    "mocha": "^2.2.5",
    "pre-commit": "^1.0.10",
    "sinon": "^1.15.3",
    "sinon-as-promised": "^4.0.0"
  },
  "dependencies": {
    "async": "^1.2.1"
  }
}
